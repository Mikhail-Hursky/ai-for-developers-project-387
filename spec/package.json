{
  "name": "booking-calendar-api-spec",
  "version": "0.1.0",
  "private": true,
  "description": "TypeSpec-описание HTTP API календаря бронирования",
  "scripts": {
    "build": "tsp compile .",
    "watch": "tsp compile . --watch"
  },
  "devDependencies": {
    "@typespec/compiler": "latest",
    "@typespec/http": "latest",
    "@typespec/openapi3": "latest"
  }
}
